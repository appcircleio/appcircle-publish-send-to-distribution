platform: Common
purpose: Publish
buildPlatform:
displayName: "Send to Distribution Module"
description: "Sends binary to Distribution Module"
runOn: "Server"
inputs:
- key: "AC_DISTRIBUTION_PROFLE_ID"
  defaultValue: ""
  isRequired: true
  title: Distribution Profile ID
  description: "Select one of the profiles defined in Distribution Module"
  helpText:
processFilename: bash
processArguments: '%AC_STEP_TEMP%/main.sh'
files:
- "main.sh"
